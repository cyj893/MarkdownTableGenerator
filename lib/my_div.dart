import 'package:flutter/material.dart';

Container verticalDiv(double height) => Container(height: height, width: 1, color: Colors.grey[300]);

Container horizontalDiv(double width) => Container(height: 1, width: width, color: Colors.grey[300]);
